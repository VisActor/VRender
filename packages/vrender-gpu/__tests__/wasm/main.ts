// import { global, createStage, container } from '@visactor/vrender';
// import { nativeEnvContainer, nativeWindowContainer, nativeCanvasContribution } from '../../src';

// // 加载native
// container.load(nativeEnvContainer);
// container.load(nativeWindowContainer);
// container.load(nativeCanvasContribution);

// global.setEnv('');
