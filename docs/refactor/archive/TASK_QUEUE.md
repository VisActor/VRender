# 任务队列 - 状态系统关键测试补充

> **文档类型**：任务执行队列
> **负责人**：架构设计师（Claude）制定，资深一线开发者执行
> **读者**：总监与协调者监控进度
> **当前阶段**：阶段二 - 关键测试补充（P1）
> **焦点**：状态系统测试（最高优先级）
> **目标**：vrender-core 测试覆盖率 15% → 40%+

---

## 👥 角色分工

- **架构设计师**：定义任务、验收标准、审查完成质量
- **资深一线开发者**：执行任务、编写测试、标记完成状态
- **总监与协调者**：监控进度、协调资源

---

## 🎯 任务组 1：状态 API 基础测试

### 任务 1.1：useStates 核心行为测试
**优先级**：🔴 P0（最高）
**预估时间**：30-45 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/graphic-state.test.ts`

**任务描述**：
创建 `graphic-state.test.ts` 文件，测试 `useStates()` 方法的核心行为。

**测试用例**：
- [x] 测试单状态应用：`graphic.useStates(['hover'])` 正确应用状态属性
- [x] 测试多状态叠加：`graphic.useStates(['hover', 'active'])` 正确合并状态属性
- [x] 测试状态优先级：后应用的状态属性覆盖先前的
- [x] 测试 `stateProxy` 动态计算：自定义 stateProxy 函数被正确调用
- [x] 测试 `hasAnimation` 参数：`true` 时触发动画，`false` 时直接应用
- [x] 测试空状态数组：`graphic.useStates([])` 清除所有状态
- [x] 测试状态应用后 `currentStates` 正确更新

**验证标准**：
- [x] 所有测试用例通过
- [x] 测试覆盖 `useStates` 方法的所有分支
- [x] 无遗漏的边界情况

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

### 任务 1.2：addState/removeState 测试
**优先级**：🔴 P0
**预估时间**：20-30 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/graphic-state.test.ts`

**任务描述**：
测试 `addState()` 和 `removeState()` 方法的行为。

**测试用例**：
- [x] 测试 `addState('hover')` 正确添加状态
- [x] 测试 `addState` 触发 `useStates` 重新计算
- [x] 测试 `removeState('hover')` 正确移除状态
- [x] 测试移除不存在的状态：不影响当前状态
- [x] 测试连续添加多个状态
- [x] 测试连续移除多个状态
- [x] 测试 `toggleState` 切换状态

**验证标准**：
- [x] 所有测试用例通过
- [x] 测试覆盖状态管理的增删改查

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

### 任务 1.3：clearStates 测试
**优先级**：🔴 P0
**预估时间**：15-20 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/graphic-state.test.ts`

**任务描述**：
测试 `clearStates()` 方法的行为和 `normalAttrs` 恢复逻辑。

**测试用例**：
- [x] 测试 `clearStates()` 清除所有当前状态
- [x] 测试清除状态后 `currentStates` 为空数组
- [x] 测试 `normalAttrs` 正确恢复：属性恢复到应用状态前的值
- [x] 测试清除状态触发属性更新
- [x] 测试清除状态时有动画 vs 无动画的区别
- [x] 测试连续清除空状态：不报错

**验证标准**：
- [x] 所有测试用例通过
- [x] 测试覆盖属性恢复逻辑

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

## 🎯 任务组 2：状态样式解析测试

### 任务 2.1：状态属性合并逻辑测试
**优先级**：🟡 P1（高）
**预估时间**：30-40 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/state-resolution.test.ts`

**任务描述**：
创建 `state-resolution.test.ts`，测试状态属性的合并和优先级逻辑。

**测试用例**：
- [x] 测试单状态属性应用：`states: { hover: { fill: 'red' } }`
- [x] 测试多状态属性合并：`hover + active` 属性正确合并
- [x] 测试属性覆盖优先级：后应用的 `active` 覆盖 `hover` 的同名属性
- [x] 测试 `stateProxy` 与静态 `states` 的优先级
- [x] 测试部分属性覆盖：只覆盖部分属性，其他属性保持
- [x] 测试嵌套属性合并：如 `style: { stroke: {...} }`
- [x] 测试 `undefined` 和 `null` 属性处理

**验证标准**：
- [x] 所有测试用例通过
- [x] 覆盖属性合并的边界情况

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

### 任务 2.2：normalAttrs 管理测试
**优先级**：🟡 P1
**预估时间**：25-35 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/normal-attrs.test.ts`

**任务描述**：
创建 `normal-attrs.test.ts`，测试 `normalAttrs` 的快照和恢复逻辑。

**测试用例**：
- [x] 测试应用状态前 `normalAttrs` 为空
- [x] 测试首次应用状态：`normalAttrs` 保存当前属性
- [x] 测试切换状态：`normalAttrs` 保持首次快照
- [x] 测试清除状态：属性恢复到 `normalAttrs`
- [x] 测试多次应用状态：`normalAttrs` 不重复保存
- [x] 测试 `updateNormalAttrs` 边界情况
- [x] 测试部分属性在 `normalAttrs` 中的处理

**验证标准**：
- [x] 所有测试用例通过
- [x] 覆盖属性快照和恢复的所有路径

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

## 🎯 任务组 3：状态与动画集成测试

### 任务 3.1：状态切换触发动画测试
**优先级**：🔴 P0
**预估时间**：40-50 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/state-animation.test.ts`

**任务描述**：
创建 `state-animation.test.ts`，测试状态切换到动画触发的完整链路。

**测试用例**：
- [x] 测试 `useStates(['hover'], true)` 触发动画
- [x] 测试 `useStates(['hover'], false)` 不触发动画
- [x] 测试动画配置 `stateAnimateConfig` 正确传递
- [x] 测试动画属性分类：`animateAttrs` vs `noAnimateAttrs`
- [x] 测试不可动画属性直接应用（如 `visible`, `zIndex`）
- [x] 测试动画完成回调触发
- [x] 测试动画中断：切换新状态时停止旧动画
- [x] 测试 `applyAnimationState` 被正确调用

**验证标准**：
- [x] 所有测试用例通过
- [x] 覆盖动画触发的主路径和边界情况

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

### 任务 3.2：动画配置优先级测试
**优先级**：🟡 P1
**预估时间**：20-30 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/state-animation.test.ts`

**任务描述**：
测试动画配置的优先级和默认值逻辑。

**测试用例**：
- [x] 测试默认动画配置：`duration: 200, easing: 'cubicOut'`
- [x] 测试自定义 `stateAnimateConfig` 覆盖默认值
- [x] 测试 context 中的 `stateAnimateConfig` 优先级
- [x] 测试禁用动画：`stateAnimateConfig: false`
- [x] 测试部分覆盖动画配置

**验证标准**：
- [x] 所有测试用例通过
- [x] 覆盖配置优先级的所有路径

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

## 🎯 任务组 4：边界情况和错误处理

### 任务 4.1：边界情况测试
**优先级**：🟢 P2（中）
**预估时间**：30-40 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/state-edge-cases.test.ts`

**任务描述**：
创建 `state-edge-cases.test.ts`，测试边界情况和异常处理。

**测试用例**：
- [x] 测试应用不存在的状态：`useStates(['nonexistent'])`
- [x] 测试 `stateProxy` 返回 `null` 或 `undefined`
- [x] 测试状态属性为空对象：`{ hover: {} }`
- [x] 测试状态名冲突：静态 `states` 和 `stateProxy` 同名
- [x] 测试极长状态名
- [x] 测试特殊字符状态名
- [x] 测试并发状态切换：快速连续调用 `useStates`

**验证标准**：
- [x] 所有测试用例通过
- [x] 覆盖异常处理路径

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

### 任务 4.2：性能和内存测试
**优先级**：🟢 P2
**预估时间**：25-35 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/state-performance.test.ts`

**任务描述**：
创建 `state-performance.test.ts`，测试状态切换的性能和内存使用。

**测试用例**：
- [x] 测试频繁状态切换不造成内存泄漏
- [x] 测试 `normalAttrs` 不重复创建
- [x] 测试大量状态切换的性能：1000 次切换 < 100ms
- [x] 测试多状态叠加的性能：10 个状态叠加 < 10ms
- [x] 测试 `stateProxy` 计算缓存（如果有）

**验证标准**：
- [x] 所有测试用例通过
- [x] 性能指标符合预期

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

## 🎯 任务组 5：Glyph 特殊处理测试

### 任务 5.1：Glyph 状态测试
**优先级**：🟢 P2
**预估时间**：20-30 分钟
**当前状态**：✅ 已完成 - 2026-03-27 - Codex
**负责人**：资深一线开发者
**文件位置**：`packages/vrender-core/__tests__/unit/graphic/glyph-state.test.ts`

**任务描述**：
创建 `glyph-state.test.ts`，测试 Glyph 的状态特殊处理逻辑。

**测试用例**：
- [x] 测试 Glyph 的 `glyphStates` 应用
- [x] 测试 Glyph 的 `glyphStateProxy` 调用
- [x] 测试状态应用到子图形 (`subGraphic`)
- [x] 测试 Glyph 状态清除
- [x] 测试 Glyph 与普通 Graphic 状态的区别

**验证标准**：
- [x] 所有测试用例通过
- [x] 覆盖 Glyph 状态的特殊路径

**完成标记**：
- ⏸️ 待开始
- 🔄 进行中 - 2026-03-27 - Codex
- ✅ 已完成 - 2026-03-27 - Codex

---

## 📊 任务完成追踪

### 统计信息
- **总任务数**：10 个
- **P0 任务**：4 个
- **P1 任务**：3 个
- **P2 任务**：3 个
- **已完成**：10 个
- **进行中**：0 个
- **待开始**：0 个

### 当前进度
```
进度：100% (10/10)
[■■■■■■■■■■]
```

---

## 🎯 执行建议

### 推荐执行顺序
1. **任务组 1**（状态 API 基础测试）→ 最关键，覆盖核心 API
2. **任务组 3**（状态与动画集成测试）→ 最重要的集成路径
3. **任务组 2**（状态样式解析测试）→ 补充样式逻辑
4. **任务组 4**（边界情况测试）→ 提高覆盖率
5. **任务组 5**（Glyph 特殊处理测试）→ 可选，优先级最低

### 并行执行建议
- 可以并行执行：任务组 1、任务组 2、任务组 4（相互独立）
- 必须串行执行：任务组 1 → 任务组 3（依赖关系）

---

## 📝 完成标准

### 任务组 1 完成
- [x] 所有 P0 测试用例通过
- [ ] 测试覆盖率 > 70%（状态 API）

### 任务组 2 完成
- [x] 所有 P1 测试用例通过
- [ ] 测试覆盖率 > 60%（样式解析）

### 任务组 3 完成
- [x] 所有 P0 测试用例通过
- [ ] 测试覆盖率 > 70%（动画集成）

### 整体完成
- [x] 所有 P0 和 P1 任务完成
- [ ] vrender-core 测试覆盖率 ≥ 40%
- [ ] 所有测试通过

---

**文档版本**：v1.0
**创建时间**：2026-03-27
**最后更新**：2026-03-27

**更新日志**：
- 2026-03-27：初始版本，创建任务队列
